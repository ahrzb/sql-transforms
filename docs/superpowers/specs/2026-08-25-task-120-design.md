ved through it, never in the map key.

## Test plan (red first, per class)

Fixtures in `packages/confit/tests/test_integer_widths.py` (extend the
existing `_KEY_DDL` block at :580):

```python
_P53 = 9007199254740992      # 2**53
_P53_1 = 9007199254740993    # 2**53+1, float() == float(_P53)
_P53_2 = 9007199254740994    # 2**53+2, exactly representable
_KEY_DDL[pa.int16()] = "SMALLINT"   # the parametrization needs all four
```

Rewritten (AC #4) -- `test_a_double_probe_against_an_integer_key_refuses_to_project_it`
becomes:

- `test_a_double_probe_against_an_integer_key_serves_the_static_value`
  Live oracle. Static c0 BIGINT {5}, probe 5.0, `SELECT s.c0 AS o`;
  asserts `got.schema == want.schema` and rows equal, plus the
  `want.schema.field("o").type == pa.int64()` "oracle moved" guard the
  neighbouring tests use.

New, one per class:

- `test_a_double_probe_key_serves_the_i64_its_double_cannot_name`
  THE regression test. Static c0 BIGINT {_P53_1} alone (unique key,
  default shape), probe `float(_P53)`. Oracle says 9007199254740993;
  a reconstruction says ...992. Fails loudly on any re-declaration
  regression, with no multiplicity involved.
- `test_a_double_probe_serves_every_integer_static_key_width`
  Parametrized over int8/int16/int32/int64 static c0 = 5, probe 5.0,
  both `SELECT s.c0 AS o` and `SELECT s.v AS o`, `JOIN` and `LEFT JOIN`.
  Schema equality against the oracle carries the width (int8 stays int8).
- `test_f64_colliding_static_keys_fan_out_under_shape_many`
  Static {(_P53,70),(_P53_1,71)}, probe `float(_P53)`, shape="many",
  `SELECT s.c0 AS o, s.v AS w`. Sorted-multiset compare, matching
  `test_duckdb_stageb_many._many_check`'s contract. Both i64s appear.
- `test_f64_colliding_static_keys_still_refuse_under_the_unique_shapes`
  Same data, default shape and shape="map" (LEFT), value-only
  projection: `pytest.raises(ValueError, match="duplicate map key")`.
  Pins that the fix did not weaken the shape contract.
- `test_a_double_probe_key_projects_through_star_and_expressions`
  `SELECT *`, `SELECT c0 AS o` (bare), `SELECT s.c0 + 1 AS o`,
  `SELECT s.c0 = 9007199254740993 AS o`, and an ON residual
  `ON k = s.c0 AND s.c0 > 3` -- each against the oracle, static
  {_P53_1}, probe `float(_P53)`.
- `test_a_using_join_keeps_merging_the_double_probe_key`
  Probe column named `c0` DOUBLE, static c0 BIGINT {_P53_1}. Oracle:
  `SELECT c0` and `SELECT *` give the probe DOUBLE 9007199254740992.0;
  `SELECT s.c0` gives BIGINT 9007199254740993. All three pinned -- this
  is the test that catches a shadow lane leaking into resolution.
- `test_a_left_miss_on_a_double_probe_key_is_still_null`
  Mirrors the existing `test_a_left_miss_on_the_key_is_still_null` for
  the lossy pairing: probe rows `[float(_P53), 1.5]`, LEFT JOIN, oracle
  says `[{o: 9007199254740993}, {o: None}]`.

Rust unit, `packages/confit/src/specializer/tests.rs`:

- `promote_key_accepts_every_integer_width_under_an_f64_probe`
- `a_lossy_key_column_is_also_a_value_lane` -- build the join, assert
  `val_cols` contains the key column exactly once and `key_cols` still
  contains it; assert a sound int/int pairing's `val_cols` does NOT.
- IR print pin for one lossy join, showing the extra probe lane.

Survive list (must stay green untouched):
`test_projected_static_key_takes_the_static_columns_width` (TASK-115,
all six parametrizations), `test_a_left_miss_on_the_key_is_still_null`,
the static-type refusal trio further down `test_integer_widths.py`,
`test_known_limitations.test_static_tables_are_frozen_unique_key_maps`,
all of `test_duckdb_stageb_many.py` including
`test_engine_order_contract`, and the wave-4/wave-5 USING and NATURAL
pins.

Gate: full pytest from the worktree root, release AND debug builds
(debug_asserts), then a 4k fuzzer campaign with residue attributed
against the pre-fix baseline -- `gen.py` emits DOUBLE columns and
integer static keys, so a batch of previously-refused queries becomes
newly served and newly compared.

## Sequencing

One branch (task-120), commits by class, each red first:

1. `key_is_lossy` + `val_cols_for` + the two call sites, with
   `key_lane` still refusing -- everything stays green, only the IR
   for an already-serving lossy join grows a lane.
2. `key_lane` reads the shadow lane; the three resolution guards land
   in the same commit (they are what keeps USING and bare names
   correct). Flips 1 and 3 pinned.
3. `promote_key` widens to every integer width. Flip 2 pinned.
4. Test rewrite (AC #4) and the survive list re-run.

Half a day. No API surface changes, no new struct fields, no lowering
edits.
