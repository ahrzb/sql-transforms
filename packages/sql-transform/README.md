# sql-transform

SQL-based data transforms, with a DataFusion-differentiated native engine and a
codegen backend.

The DuckDB serving engine that used to live here is now the [`confit`](../confit)
package; `SpecializedTransform` builds on it. See the repository root README for
the project overview.
